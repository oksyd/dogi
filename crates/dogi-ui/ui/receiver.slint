export component BoltReceiverDrawing inherits Rectangle {
    background: transparent;

    Image {
        width: parent.width;
        height: parent.height;
        source: @image-url("assets/logi-bolt-receiver.png");
        image-fit: contain;
    }
}
